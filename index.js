// Multiplication equation whose result is 62
const num1 = 31
const num2 = 2

const multiply = num1*num2

// Generates a random integer greater than 0
const random = Math.floor((Math.random() * 10 ) + 1) 

// Remainder to equal 4
const num3 = 18
const num4 = 7

const mod = num3%num4

// Returns 20 as the maximum number
const list = (6,9,8,7,20)

const max = Math.max(list)